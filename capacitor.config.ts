import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tranglo.assessment',
  appName: 'tranglo-frontend-assessment',
  webDir: 'dist/tranglo-frontend-assessment/browser'
};

export default config;
